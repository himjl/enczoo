[project]
name = "enczoo"
version = "0.1.1"
description = "Map images (as `PIL.Images`) to intermediate representations (as `np.ndarray`) from off-the-shelf vision models."
readme = "README.md"
authors = [
    { name = "Michael J. Lee", email = "mil@mit.edu" }
]
license = { file = "LICENSE" }
requires-python = ">=3.12"
dependencies = [
    "clip",
    "pillow>=12.1.0",
    "torch>=2.9.1",
    "torchvision>=0.24.1",
]

[build-system]
requires = ["uv_build>=0.8.12,<0.9.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "black>=25.12.0",
    "matplotlib>=3.10.8",
    "pytest>=9.0.2",
    "ruff>=0.14.10",
    "ty>=0.0.9",
]

[tool.uv.sources]
clip = { git = "https://github.com/openai/CLIP.git", rev = "ded190a052fdf4585bd685cee5bc96e0310d2c93" }
